# AI for the Win - Dependencies
# Security AI Training Program
#
# REQUIREMENTS: Python 3.10 or higher
# (Python 3.9 is no longer supported due to Gradio 6.x security requirements)
#
# Install with: pip install -r requirements.txt
# Verify setup: python scripts/verify_setup.py
#
# TROUBLESHOOTING:
# If you get dependency errors, try these in order:
#   1. pip install --upgrade pip setuptools wheel
#   2. pip install -r requirements.txt
#
# Or use uv for faster, more reliable resolution:
#   pip install uv && uv pip install -r requirements.txt
#
# Last updated: January 2026

# =============================================================================
# CORE ML/AI LIBRARIES
# =============================================================================
numpy>=1.24.0,<2.0.0
pandas>=2.0.0,<3.0.0
scikit-learn>=1.3.0,<2.0.0
scipy>=1.11.0,<2.0.0

# Deep Learning (PyTorch only - no TensorFlow to avoid conflicts)
torch>=2.0.0,<3.0.0

# =============================================================================
# LLM FRAMEWORKS & ORCHESTRATION
# =============================================================================
# LangChain ecosystem - TIGHTLY PINNED to 1.x series (Jan 2026 stable)
# These packages MUST use compatible versions
langchain>=1.0.0,<2.0.0
langchain-anthropic>=1.0.0,<2.0.0
langchain-openai>=1.0.0,<2.0.0  # v1.x required for langchain 1.x compatibility
langchain-google-genai>=4.0.0,<5.0.0  # v4.x required for langchain 1.x compatibility
langchain-community>=0.3.0,<1.0.0
langgraph>=1.0.0,<2.0.0

# Direct LLM SDKs - updated to Jan 2026 versions
anthropic>=0.70.0,<1.0.0
openai>=1.50.0,<2.0.0
google-generativeai>=0.8.0,<1.0.0

# Unified LLM API (supports 100+ providers)
litellm>=1.50.0,<2.0.0

# Structured outputs from LLMs
instructor>=1.5.0,<2.0.0

# =============================================================================
# EMBEDDINGS & VECTOR DATABASES
# =============================================================================
# Tightly constrained to avoid cross-package conflicts
chromadb>=1.0.0,<2.0.0
sentence-transformers>=5.0.0,<6.0.0

# Hugging Face ecosystem - match sentence-transformers requirements
transformers>=4.40.0,<5.0.0
huggingface-hub>=0.25.0,<1.0.0  # transformers 4.x still requires <1.0

# NOTE: tf-keras REMOVED - it pulls TensorFlow which conflicts with PyTorch
# If you specifically need Keras 3, install separately: pip install tf-keras

# =============================================================================
# NLP & TEXT PROCESSING
# =============================================================================
nltk>=3.8.0,<4.0.0
tiktoken>=0.7.0,<1.0.0
# spacy - COMMENTED OUT due to frequent numpy/scipy conflicts
# Uncomment if needed for specific labs: pip install spacy
# spacy>=3.7.0,<4.0.0

# =============================================================================
# SECURITY TOOLS
# =============================================================================
yara-python>=4.3.0,<5.0.0
pefile>=2023.2.7

# =============================================================================
# API & WEB FRAMEWORKS
# =============================================================================
requests>=2.31.0,<3.0.0
python-dotenv>=1.0.0,<2.0.0
aiohttp>=3.9.0,<4.0.0
httpx>=0.27.0,<1.0.0

# Web frameworks
fastapi>=0.110.0,<1.0.0
uvicorn>=0.27.0,<1.0.0

# UI frameworks
streamlit>=1.35.0,<2.0.0
gradio>=6.1.0,<7.0.0  # v6.1.0 minimum required for CVE security fixes

# =============================================================================
# DOCUMENT PROCESSING (lightweight only)
# =============================================================================
pypdf>=4.0.0,<5.0.0
docx2txt>=0.8

# NOTE: unstructured REMOVED - notorious for 100+ transitive dependencies
# causing pip resolution failures. Install separately if needed:
# pip install "unstructured[all-docs]"

# =============================================================================
# CLI & VISUALIZATION
# =============================================================================
rich>=13.7.0,<14.0.0
typer>=0.12.0,<1.0.0
matplotlib>=3.8.0,<4.0.0
seaborn>=0.13.0,<1.0.0
plotly>=5.20.0,<6.0.0
networkx>=3.2.0,<4.0.0

# NOTE: kaleido has Windows installation issues - install separately if needed:
# pip install kaleido

# =============================================================================
# JUPYTER & INTERACTIVE
# =============================================================================
notebook>=7.0.0,<8.0.0
ipywidgets>=8.1.0,<9.0.0
tqdm>=4.66.0,<5.0.0

# =============================================================================
# DATA VALIDATION & SERIALIZATION
# =============================================================================
pydantic>=2.5.0,<3.0.0
pyyaml>=6.0.0,<7.0.0

# =============================================================================
# TESTING & CODE QUALITY
# =============================================================================
pytest>=8.0.0,<9.0.0
pytest-cov>=4.1.0,<6.0.0
pytest-asyncio>=0.23.0,<1.0.0
black>=24.0.0,<25.0.0
flake8>=7.0.0,<8.0.0
mypy>=1.8.0,<2.0.0
isort>=5.13.0,<6.0.0
bandit>=1.7.0,<2.0.0

# =============================================================================
# OPTIONAL PACKAGES - Install separately to avoid dependency conflicts
# =============================================================================
# These are commented out because they significantly increase resolution time
# or have known compatibility issues. Install individually as needed:
#
# Heavy NLP:
#   pip install "spacy>=3.7.0"
#
# Document processing (100+ dependencies):
#   pip install "unstructured[all-docs]"
#
# TensorFlow/Keras (conflicts with PyTorch):
#   pip install tf-keras
#
# Plotly export (Windows issues):
#   pip install kaleido
#
# Multi-agent frameworks:
#   pip install crewai
#   pip install autogen
#
# Memory systems:
#   pip install mem0ai
#
# Prompt optimization:
#   pip install dspy-ai
